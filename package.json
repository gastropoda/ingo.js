{
  "name": "ingo.js",
  "private": true,
  "devDependencies": {
    "http-server": "^0.6.1",
    "karma": "^0.12.28",
    "karma-mocha": "^0.1.9",
    "karma-mocha-reporter": "^0.3.1",
    "karma-phantomjs-launcher": "^0.1.4",
    "karma-sinon-chai": "^0.2.0",
    "mocha": "^2.0.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server app -a localhost -p 8000 -c-1 -s",
    "test": "karma start"
  }
}
